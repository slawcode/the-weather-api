document.getElementById('searchForACity').addEventListener('click', function() {
    var city = document.getElementByIf('searchForACity').value;
    var apiKey = ;
    var apiUrl = ;

    fetch(apiUrl).then(response => response.json())
    
    .then(data => {
        if (data.code === 200) {
            var weatherResult = `
            <`
        }
    })
})